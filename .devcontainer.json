{
    "name": "Python Development Environment",
    "build": {
        "context": "..",
        "dockerfile": ".devcontainer/Dockerfile",
        "args": {
            "VARIANT": "3.8"
        }
    },
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "postCreateCommand": "mkdir -p ./ny_taxi_cassandra && pip install -r requirements.txt && docker run --name=cass-docker -p 9042:9042 -e CASSANDRA_USER=cassandra -e CASSANDRA_PASSWORD=admin123 -v $(pwd)/ny_taxi_cassandra:/var/lib/cassandra -d cassandra",
    "extensions": [
        "ms-python.python"
    ],
    "remoteUser": "vscode"
}
